{
  "mcpServers": {
    "notion": {
      "command": "npx",
      "args": ["-y", "@notionhq/notion-mcp-server", "--transport", "stdio"],
      "env": {
            "NOTION_TOKEN": "${NOTION_TOKEN}"
      }
    }
  }
}